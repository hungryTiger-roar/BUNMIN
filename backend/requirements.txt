# Aunion AI Backend 의존성

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0

# AI/ML Core
numpy>=1.24.0
torch>=2.0.0
transformers>=4.50.0
accelerate>=0.25.0
safetensors>=0.4.0
sentencepiece>=0.1.99
protobuf>=4.25.0
peft>=0.7.0
bitsandbytes>=0.41.0
python-dotenv>=1.0.0

# ASR (음성 인식)
soundfile>=0.12.0
librosa>=0.10.0

# TTS (음성 합성)
piper-tts>=1.2.0

# OCR (텍스트 추출)
easyocr>=1.7.0
rapidocr-onnxruntime>=1.3.0
Pillow>=10.0.0

# PDF 처리
pymupdf>=1.23.0

# 유틸리티
pydantic>=2.5.0
tqdm>=4.66.0
requests>=2.31.0
huggingface-hub>=0.20.0

# 평가 지표
nltk>=3.8.0
bert-score>=0.3.13
unbabel-comet>=2.2.7  # XCOMET-XL 평가 시 필요 (--xcomet 플래그), HuggingFace 접근 승인 필요

# 평가 샘플 생성
edge-tts>=6.1.0

# GPU 사용 시 torch를 CUDA 버전으로 재설치:
# CUDA 11.8 → cu118 / CUDA 12.1 → cu121 / CUDA 12.4 이상 or 13.x → cu124
# pip install torch --index-url https://download.pytorch.org/whl/cu124
# 디바이스 설정은 프로젝트 루트 .env 파일에서 관리 (docs/setting/환경설정.md 참고)
