# Aunion AI Backend 의존성

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0

# AI/ML Core
numpy>=1.24.0,<2.0.0  # unbabel-comet, paddlex 호환성
# torch, torchvision은 npm run setup에서 CUDA 버전으로 설치 (requirements.txt에서 제외)
transformers>=4.50.0
accelerate>=0.25.0
safetensors>=0.4.0
sentencepiece>=0.1.99
protobuf>=4.25.0
peft>=0.10.0
bitsandbytes>=0.46.1
python-dotenv>=1.0.0

# ASR (음성 인식)
faster-whisper>=1.0.0
soundfile>=0.12.0
librosa>=0.10.0

# TTS (음성 합성)
piper-tts>=1.2.0

# OCR (텍스트 추출)
easyocr>=1.7.0
rapidocr-onnxruntime>=1.3.0
surya-ocr>=0.17.0  # Transformer 기반 OCR (한글 정확도 향상), torch>=2.7.0 필요 (cu126 사용)
# paddleocr - Windows protobuf 충돌로 제외 (필요 시 수동 설치)
Pillow>=10.0.0,<12.0.0  # surya-ocr 호환성
opencv-python>=4.8.0  # Inpainting (번역 오버레이 시 배경 자연스럽게 복원)

# PDF 처리
pymupdf>=1.23.0

# 유틸리티
pydantic>=2.5.0
tqdm>=4.66.0
requests>=2.31.0
huggingface-hub>=0.20.0
pyyaml>=6.0.0  # config.yaml 설정 파일 로드

# 평가 지표
nltk>=3.8.0
bert-score>=0.3.13
unbabel-comet>=2.2.7  # XCOMET-XL 평가 시 필요 (--xcomet 플래그), HuggingFace 접근 승인 필요

# 평가 샘플 생성
edge-tts>=6.1.0

# GPU 사용 시 torch를 CUDA 버전으로 재설치:
# CUDA 12.6+ → cu126 (surya-ocr 0.17+ 호환)
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
# 디바이스 설정은 프로젝트 루트 .env 파일에서 관리 (docs/setting/환경설정.md 참고)
